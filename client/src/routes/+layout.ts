// This tells SvelteKit to handle these routes as fallbacks
export const trailingSlash = 'never';

// Prerender by default
export const prerender = true;

// Enable client-side routing
export const csr = true;
